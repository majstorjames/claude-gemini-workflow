# Gemini Review: <Plan title>

<!-- Review as an INDEPENDENT reviewer, not a second author: assume the plan is flawed and find
     where. Prioritize what the author is blind to, not what they already optimized against:
       - Architecture & system fit (parallel/conflicting patterns, layering, boundaries)
       - Requirement & scope gaps (missing edge cases, error/failure/rollback paths, assumptions)
       - Integration & contracts (API/schema/migration/backward-compat with the rest of the repo)
       - Simplicity (is there a materially simpler approach for the same requirement?)
       - Correctness & security (in scope, but don't stop here)
     Verify every claim against the ACTUAL codebase (grep/read), not the plan text. -->

**Status: APPROVED / APPROVED WITH SUGGESTIONS 🟡 / REJECTED**

<!-- One-paragraph summary: what's strong, what's the overall verdict. -->

### 1. <Finding title>
<!-- What the issue is, anchored to a file/function. -->
**Fix:** <concrete, actionable directive.>

### 2. <Finding title>
<!-- ... -->
**Fix:** <...>

<!-- Close with a short note, e.g. "Please review these and implement if you agree." -->
